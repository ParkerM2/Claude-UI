# Claude-UI Environment Variables
# Copy this file to .env and customize as needed

# ============================================================
# Claude API Configuration
# ============================================================

# Anthropic API key for direct Claude API access
# Get yours at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-...

# ============================================================
# OAuth Provider Credentials
# ============================================================
# Configure OAuth providers for third-party integrations.
# Each provider requires both client ID and secret from their developer console.

# Google OAuth (Calendar, Gmail integration)
# Create credentials at: https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# Spotify OAuth (Music playback control)
# Create app at: https://developer.spotify.com/dashboard
# SPOTIFY_CLIENT_ID=your_spotify_client_id
# SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# GitHub OAuth (Repository access, PRs, issues)
# Create OAuth app at: https://github.com/settings/developers
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret

# Slack OAuth (Webhook commands, notifications)
# Create app at: https://api.slack.com/apps
# SLACK_CLIENT_ID=your_slack_client_id
# SLACK_CLIENT_SECRET=your_slack_client_secret

# Discord OAuth (Bot commands, notifications)
# Create app at: https://discord.com/developers/applications
# DISCORD_CLIENT_ID=your_discord_client_id
# DISCORD_CLIENT_SECRET=your_discord_client_secret

# ============================================================
# Hub Server Configuration (optional)
# ============================================================
# Only needed if running the hub server component

# Hub API key for authenticated access
# Generate a secure key: openssl rand -hex 32
# HUB_API_KEY=your_hub_api_key

# Hub server port (default: 3200)
# PORT=3200

# Hub server host (default: 0.0.0.0)
# HOST=0.0.0.0

# Bootstrap secret for initial API key generation
# Required for POST /api/auth/generate-key
# Generate: openssl rand -hex 32
# HUB_BOOTSTRAP_SECRET=your_bootstrap_secret

# Allowed CORS origins (comma-separated)
# Defaults to localhost and 127.0.0.1
# HUB_ALLOWED_ORIGINS=https://my-app.example.com

# ============================================================
# Development & Debugging
# ============================================================

# Shell path override (uses system default if not set)
# Windows: COMSPEC (default: cmd.exe)
# macOS/Linux: SHELL (default: /bin/bash)

# Home directory override (uses system default if not set)
# HOME=/Users/your-username
# USERPROFILE=C:\Users\your-username

# Electron development server URL (set automatically by electron-vite)
# ELECTRON_RENDERER_URL=http://localhost:5173
