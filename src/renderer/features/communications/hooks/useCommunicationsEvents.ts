/**
 * Communications event listeners
 *
 * Placeholder for future IPC event subscriptions
 * when communication services emit connection status events.
 */

export function useCommunicationsEvents() {
  // Will subscribe to MCP connection state events when wired
}
