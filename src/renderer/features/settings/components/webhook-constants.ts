/**
 * Shared constants for webhook settings components.
 *
 * NOTE: Raw CSS class constants (INPUT_CLASS, SAVE_BUTTON_CLASS) removed
 * after migration to design system primitives (Button, Input from @ui).
 */
