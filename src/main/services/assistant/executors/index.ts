/**
 * Command executor barrel.
 *
 * Re-exports the public API: createCommandExecutor factory,
 * CommandExecutor interface, and CommandExecutorDeps type.
 */

export type { CommandExecutorDeps } from './types';
export { routeIntent } from './router';
